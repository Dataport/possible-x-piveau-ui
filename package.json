{
  "name": "piveau-ui",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "npm run dev -w modules-demo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
